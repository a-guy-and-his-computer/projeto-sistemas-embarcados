services:
  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: terraguard_broker
    ports:
      - "1883:1883"
    volumes:
      # O Docker vai "puxar" o arquivo que acabamos de criar para dentro do container
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped